---
id: {{ source_file.slug }}
title: {{ source_file.path }}
sidebar_label: {{ source_file.path }}
---

import SourceCodeBlock from '@site/src/components/SourceCodeBlock';

# `{{ source_file.path }}`

{% if source_file.available %}

<SourceCodeBlock lines={ {{ source_file_lines_json | safe }} } />

{% else %}

_Source file unavailable locally._

The knowledge graph referenced this path, but the generator could not read the file from:

`{{ repo_path }}/{{ source_file.path }}`

Symbol links fall back to this placeholder page so navigation never breaks.

{% endif %}

---

**Related pages:** [Source Browser]({{ base_path }}/source) · [{{ project.name }} Home]({{ base_path }})
