name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  shellcheck:
    name: Shell lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run shellcheck
        run: |
          SHELL_SCRIPTS=$(find . -name "*.sh" -not -path "./vendor/*" -not -path "./node_modules/*")
          if [ -n "$SHELL_SCRIPTS" ]; then
            shellcheck -x -s bash --severity=error $SHELL_SCRIPTS
          else
            echo "No shell scripts found"
          fi

  commitlint:
    name: Conventional Commits
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install commitlint
        run: npm install -g @commitlint/cli @commitlint/config-conventional

      - name: Create commitlint config
        run: |
          cat > commitlint.config.js << 'EOF'
          module.exports = {
            extends: ['@commitlint/config-conventional'],
            rules: {
              'type-enum': [2, 'always', [
                'feat', 'fix', 'docs', 'style', 'refactor',
                'test', 'chore', 'ci', 'perf', 'build', 'revert'
              ]],
              'subject-max-length': [2, 'always', 120],
            },
          };
          EOF

      - name: Lint PR commits
        run: npx commitlint --from ${{ github.event.pull_request.base.sha }} --to ${{ github.event.pull_request.head.sha }} --verbose

  mirror-sync:
    name: Mirror sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Regenerate platform mirrors from .claude/commands
        run: python3 scripts/generate-mirrors.py

      - name: Fail if mirrors drifted
        run: |
          if ! git diff --exit-code -- .codex .opencode .gemini .pi; then
            echo "::error::Platform mirrors are out of sync with .claude/commands/."
            echo "Edit .claude/commands/*.md only, then run: python3 scripts/generate-mirrors.py"
            echo "and commit the regenerated mirrors together with the source change."
            exit 1
          fi

  readme-parity:
    name: README parity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check translations against skills/ and README.md
        run: node scripts/check-readme-parity.js

      - name: Check landing page i18n dictionaries
        run: node scripts/check-i18n.js

  links:
    name: Link check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run lychee
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --exclude-loopback
            --exclude "https://opencode.ai"
            --exclude "https://pi.dev"
            --exclude-mail
            --no-progress
            "**/*.md"
          fail: false
