# Shell scripts must keep LF endings — bash (including Git Bash on Windows)
# cannot execute CRLF scripts, and core.autocrlf=true would otherwise convert
# them on checkout.
*.sh text eol=lf
setup.sh text eol=lf
